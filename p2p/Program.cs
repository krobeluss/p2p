using System.Net;
using System.Threading;

namespace P2P
{

    class Program
    {
        static void Main(string[] args)
        {
            //PrivateNetwork privateNetwork = new PrivateNetwork(new IPEndPoint(IPAddress.Parse("0.0.0.0"), 9963));
            //Thread.Sleep(1000);
            //privateNetwork.Dispose();

        }
    }
}
